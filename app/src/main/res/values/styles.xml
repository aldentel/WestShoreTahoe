<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">true</item>
        <item name="colorPrimary">#039BE5</item>
        <item name="colorPrimaryDark">#0277BD</item>
        <item name="colorAccent">#84FFFF</item>
    </style>

    <!-- Style for Image layout -->
    <style name="ImageStyle">
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_height">120dp</item>
    </style>

    <!-- Style for tab layout -->
    <style name="TahoeTabLayout">
        <item name="tabIndicatorColor">#84FFFF</item>
        <item name="tabTextColor">#0277BD</item>
        <item name="tabSelectedTextColor">@android:color/black</item>
    </style>

    <style name="WordStyle">
        <item name="android:layout_width">240dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_marginLeft">122dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
